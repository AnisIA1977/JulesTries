package com.sicam.logops.repository;

import com.sicam.logops.model.LogisticRequest;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogisticRequestRepository extends JpaRepository<LogisticRequest, Long> {
}
